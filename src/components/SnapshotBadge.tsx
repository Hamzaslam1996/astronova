export default function SnapshotBadge({ what }: { what: string }) {
  return (
    <span className="ml-2 rounded bg-amber-500/10 border border-amber-600/40 text-amber-300 px-2 py-0.5 text-[10px]">
      Snapshot
    </span>
  );
}
